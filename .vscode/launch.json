{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node-terminal",
            "request": "launch",
            "name": "Locket: Build and Run iOS Simulator",
            "command": "\"${workspaceFolder}/scripts/run-simulator.sh\"",
            "cwd": "${workspaceFolder}"
        }
    ]
}
